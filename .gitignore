node_modules/
dist/
.env
# Lokalni env override-i (per-mašina): .env.local, .env.development.local, ...
.env*.local
slike gresaka/

# Lokalni prod schema dump (sadrži service_role JWT) — regeneriši: supabase db dump --linked --schema public
supabase/_remote_schema_snapshot.sql

# Supabase CLI interni temp / lokalno stanje
supabase/.temp/
supabase/.branches/

# Lokalna Claude Code podešavanja (per-mašina)
.claude/settings.local.json
